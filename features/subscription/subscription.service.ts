import { TierId } from "@/config/tier";

/**
 * This will later wrap Apple StoreKit.
 * For now, it is intentionally boring.
 */
export async function purchaseSubscription(tier: TierId) {
  // stub
  return true;
}

export async function restorePurchases() {
  // stub
  return true;
}
